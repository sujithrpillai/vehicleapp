apiVersion: v1
kind: Service
metadata:
  labels:
    app: frontend
    version: blue
  name: frontend-prod
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: frontend
    version: blue
  type: LoadBalancer
status:
  loadBalancer: {}